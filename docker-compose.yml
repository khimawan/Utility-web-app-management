version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: utility-management-app
    ports:
      - "3000:3000"
    volumes:
      # Persist database data
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
