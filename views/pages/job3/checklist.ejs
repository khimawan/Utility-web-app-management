<%- include('../../partials/header') %>
  <%- include('../../partials/topnav') %>
  <div class="page-content">
    <div class="page-header d-flex justify-content-between align-items-center">
      <h4><i class="bi bi-clipboard-check me-2"></i><%= categoryName %></h4>
      <a href="/job3" class="btn btn-sm btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Kembali</a>
    </div>

    <div class="row">
      <div class="col-md-5">
        <div class="card">
          <div class="card-header"><h5>Input Checklist</h5></div>
          <div class="card-body">
            <form method="POST" action="/job3/checklist/<%= category %>" enctype="multipart/form-data" id="checklistForm">
              <% if (category === 'pemakaian_air' || category === 'energi_listrik' || category === 'pemakaian_gas' || category === 'suhu_trafo') { %>
                <div class="mb-3">
                  <label class="form-label">Pilih PIC</label>
                  <select name="machine_id" class="form-select" required>
                    <option value="">-- Pilih PIC --</option>
                    <% members.forEach(function(m) { %>
                      <option value="<%= m.id %>" <%= m.id === user.id ? 'selected' : '' %>><%= m.name %></option>
                    <% }); %>
                  </select>
                </div>
                <div class="mb-3">
                  <label class="form-label">Shift</label>
                  <select name="shift" class="form-select" required>
                    <option value="1">Shift 1</option>
                    <option value="2">Shift 2</option>
                    <option value="3">Shift 3</option>
                  </select>
                </div>

                <% if (category === 'energi_listrik') { %>
                  <% templates.forEach(function(t) { %>
                    <% if (t.parameter_name.indexOf('147 KVA') >= 0 && t.sort_order === 3) { %>
                      <hr class="my-3">
                      <h6 class="text-primary fw-bold">147 KVA</h6>
                    <% } else if (t.parameter_name.indexOf('197 KVA') >= 0 && t.sort_order === 5) { %>
                      <hr class="my-3">
                      <h6 class="text-primary fw-bold">197 KVA</h6>
                    <% } else if (t.parameter_name.indexOf('555 KVA') >= 0 && t.sort_order === 7) { %>
                      <hr class="my-3">
                      <h6 class="text-primary fw-bold">555 KVA</h6>
                    <% } else if (t.parameter_name.indexOf('Capacitor Bank') >= 0 && t.sort_order === 9) { %>
                      <hr class="my-3">
                      <h6 class="text-primary fw-bold">Capacitor Bank</h6>
                    <% } %>
                  <div class="mb-3">
                    <label class="form-label"><%= t.parameter_name.replace('147 KVA - ', '').replace('197 KVA - ', '').replace('555 KVA - ', '') %> <% if (t.unit) { %>(<%= t.unit %>)<% } %></label>
                    <% if (t.parameter_type === 'date') { %>
                      <input type="date" name="values" class="form-control">
                    <% } else if (t.parameter_type === 'time') { %>
                      <select name="values" class="form-select">
                        <option value="">-- Pilih Jam --</option>
                        <option value="00:00">00:00</option><option value="01:00">01:00</option><option value="02:00">02:00</option><option value="03:00">03:00</option>
                        <option value="04:00">04:00</option><option value="05:00">05:00</option><option value="06:00">06:00</option><option value="07:00">07:00</option>
                        <option value="08:00">08:00</option><option value="09:00">09:00</option><option value="10:00">10:00</option><option value="11:00">11:00</option>
                        <option value="12:00">12:00</option><option value="13:00">13:00</option><option value="14:00">14:00</option><option value="15:00">15:00</option>
                        <option value="16:00">16:00</option><option value="17:00">17:00</option><option value="18:00">18:00</option><option value="19:00">19:00</option>
                        <option value="20:00">20:00</option><option value="21:00">21:00</option><option value="22:00">22:00</option><option value="23:00">23:00</option>
                      </select>
                    <% } else if (t.parameter_type === 'number') { %>
                      <input type="number" step="any" name="values" class="form-control" min="<%= t.min_value %>" max="<%= t.max_value %>">
                    <% } else { %>
                      <input type="text" name="values" class="form-control">
                    <% } %>
                    <input type="hidden" name="templates" value="<%= t.id %>">
                  </div>
                  <% }); %>
                <% } else { %>
                  <% templates.forEach(function(t) { %>
                  <div class="mb-3">
                    <label class="form-label"><%= t.parameter_name %> <% if (t.unit) { %>(<%= t.unit %>)<% } %></label>
                    <% if (t.parameter_type === 'date') { %>
                      <input type="date" name="values" class="form-control">
                    <% } else if (t.parameter_type === 'time') { %>
                      <select name="values" class="form-select">
                        <option value="">-- Pilih Jam --</option>
                        <option value="00:00">00:00</option><option value="01:00">01:00</option><option value="02:00">02:00</option><option value="03:00">03:00</option>
                        <option value="04:00">04:00</option><option value="05:00">05:00</option><option value="06:00">06:00</option><option value="07:00">07:00</option>
                        <option value="08:00">08:00</option><option value="09:00">09:00</option><option value="10:00">10:00</option><option value="11:00">11:00</option>
                        <option value="12:00">12:00</option><option value="13:00">13:00</option><option value="14:00">14:00</option><option value="15:00">15:00</option>
                        <option value="16:00">16:00</option><option value="17:00">17:00</option><option value="18:00">18:00</option><option value="19:00">19:00</option>
                        <option value="20:00">20:00</option><option value="21:00">21:00</option><option value="22:00">22:00</option><option value="23:00">23:00</option>
                      </select>
                    <% } else if (t.parameter_type === 'number') { %>
                      <input type="number" step="any" name="values" class="form-control" min="<%= t.min_value %>" max="<%= t.max_value %>">
                    <% } else { %>
                      <input type="text" name="values" class="form-control">
                    <% } %>
                    <input type="hidden" name="templates" value="<%= t.id %>">
                  </div>
                  <% }); %>
                <% } %>

                <% if (category !== 'pemakaian_air' && category !== 'pemakaian_gas' && category !== 'suhu_trafo') { %>
                <hr class="my-3">
                <div class="mb-3">
                  <label class="form-label">Foto Dokumentasi (JPEG/PNG)</label>
                  <input type="file" name="foto_dokumentasi" class="form-control" accept=".jpg,.jpeg,.png">
                  <div class="form-text">Format: JPG, JPEG, PNG. Maks 5MB.</div>
                </div>
                <% } %>
                <hr class="my-3">
                <div class="d-flex gap-2">
                  <button type="submit" class="btn btn-primary flex-fill"><i class="bi bi-check-circle me-1"></i>Submit</button>
                  <button type="button" class="btn btn-secondary flex-fill" onclick="document.getElementById('checklistForm').reset()"><i class="bi bi-x-circle me-1"></i>Clear Form</button>
                </div>
              <% } else { %>
                <div class="mb-3">
                  <label class="form-label">Tanggal</label>
                  <input type="date" name="entry_date" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Shift</label>
                  <select name="shift" class="form-select" required>
                    <option value="1">Shift 1</option>
                    <option value="2">Shift 2</option>
                    <option value="3">Shift 3</option>
                  </select>
                </div>
                <% templates.forEach(function(t) { %>
                <div class="mb-3">
                  <label class="form-label"><%= t.parameter_name %> <% if (t.unit) { %>(<%= t.unit %>)<% } %></label>
                  <% if (t.parameter_type === 'number') { %>
                    <input type="number" step="any" name="values" class="form-control" min="<%= t.min_value %>" max="<%= t.max_value %>">
                  <% } else { %>
                    <input type="text" name="values" class="form-control">
                  <% } %>
                  <input type="hidden" name="templates" value="<%= t.id %>">
                </div>
                <% }); %>
                <div class="mb-3">
                  <label class="form-label">Catatan</label>
                  <textarea name="notes" class="form-control" rows="2"></textarea>
                </div>
                <div class="mb-3">
                  <label class="form-label">Foto Dokumentasi (JPEG/PNG)</label>
                  <input type="file" name="foto_dokumentasi" class="form-control" accept=".jpg,.jpeg,.png">
                  <div class="form-text">Format: JPG, JPEG, PNG. Maks 5MB.</div>
                </div>
                <button type="submit" class="btn btn-primary w-100"><i class="bi bi-check-circle me-1"></i>Simpan Checklist</button>
              <% } %>
            </form>
          </div>
        </div>
      </div>

      <div class="col-md-7">
        <div class="card">
          <div class="card-header"><h5>Riwayat Checklist</h5></div>
          <div class="card-body">
            <div class="table-responsive">
              <table class="table table-sm table-striped">
                <thead>
                  <tr>
                    <th>Tanggal</th>
                    <th>Shift</th>
                    <% if (category === 'pemakaian_air' || category === 'energi_listrik' || category === 'pemakaian_gas' || category === 'suhu_trafo') { %>
                      <th>PIC</th>
                    <% } %>
                    <th>Oleh</th>
                    <th>Aksi</th>
                  </tr>
                </thead>
                <tbody>
                  <% entries.forEach(function(e) { %>
                  <tr>
                    <td><%= e.entry_date %></td>
                    <td><span class="badge bg-primary">Shift <%= e.shift %></span></td>
                    <% if (category === 'pemakaian_air' || category === 'energi_listrik' || category === 'pemakaian_gas' || category === 'suhu_trafo') { %>
                      <td><%= e.pic_name || '-' %></td>
                    <% } %>
                    <td><%= e.input_by_name %></td>
                    <td>
                      <form method="POST" action="/job3/checklist/entry/<%= e.id %>/delete" style="display:inline;" class="delete-btn-form">
                        <button type="submit" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                      </form>
                    </td>
                  </tr>
                  <% }); %>
                  <% if (entries.length === 0) { %>
                  <tr>
                    <td colspan="<%= (category === 'pemakaian_air' || category === 'energi_listrik' || category === 'pemakaian_gas' || category === 'suhu_trafo') ? 5 : 4 %>" class="text-center text-muted">Belum ada data</td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('../../partials/footer') %>
